{
  "name": "airsoft-club-bot",
  "version": "2.0.0",
  "description": "Airsoft Club — Telegram Bot + Mini App",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:webapp": "cd webapp && npm install && npm run build",
    "setup": "npm install && cd webapp && npm install && npm run build"
  },
  "dependencies": {
    "grammy": "^1.21.1",
    "mysql2": "^3.9.0",
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
